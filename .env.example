# Required for Auth.js sessions (generate with: openssl rand -base64 32)
AUTH_SECRET=

# Family login (protects the whole calendar)
FAMILY_USERNAME=family
FAMILY_PASSWORD=change-me-to-a-strong-password

# Optional: full site URL in production (Auth.js)
# AUTH_URL=https://your-app.vercel.app
